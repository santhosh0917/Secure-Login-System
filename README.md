# Secure-Login-System
A secure login system built with user authentication, hashed passwords (bcrypt/Argon2), input validation, and SQL injection protection. Features secure session management with login/logout functionality and optional Two-Factor Authentication (2FA). Designed to enhance account security and protect users from common web attacks.
